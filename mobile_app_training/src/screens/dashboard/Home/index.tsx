import { Box, Text } from "native-base";

import { Layout } from "@src/components/Layout";

export const Home = () => {
  return (
    <Layout headerType={"TAB"} >
      <Box>
        <Text>HOME</Text>
      </Box>
    </Layout>
  );
};
