import { Box, Text } from "native-base";

export const Home = () => {
  return (
    <Box display="flex" flex={1} alignItems="center" justifyContent="center">
      <Text>HOME</Text>
    </Box>
  );
};
